# ValueBet · 项目行为约定

足球赛事概率建模 + 投注期望值计算系统。FastAPI + SQLite + React。

## 🔴 最重要的事实

**模型对市场价格的增量信息为零。这条建模路线已经关闭，不是「还差一点」。**

141,287 场严格样本外走查（2013-2025，38 个联赛，每月重拟合）的结论：

- 模型 RPS 0.2104 vs 市场 0.2046，配对差 +0.00585 ± 0.00015，**t = +38.5**
- **27 个参数池，没有一个跑赢市场**（含英格兰 5 个级别、苏格兰 4 个级别）
- 增量信息检验（结果 ~ 市场对数几率 + 模型对数几率，样本外）：**t = -0.2，零增量**。
  该检验能在 3σ 上检出比模型自身缺口小 200 倍的优势，结果什么都没有
- CORP 分解：缺口 100% 来自区分力，**模型的校准比市场还好**——加校准层拿不回任何东西

详见 `handoff/09_WALKFORWARD_FINDINGS.md`（方法与全部数字），
`handoff/08_MARKET_VALIDATION.md`（上一轮，结论方向正确但样本量不足）。

**已被证伪、不要再试的方向：**

- 主客场分离攻防参数（08 号列的第一优先级）——**显著更差**，+0.00268，t=+21.6
- 大小球 2.5 市场——增量信息 t=+0.7，无
- **亚洲让球盘**（抽水最便宜，4.15% vs 1X2 的 5.78%）——ROI -3.2%〜-3.5%，
  增量信息 t=+0.7，无
- **角球等冷门市场**——免费源不存在历史角球/牌赔率（已排查确认，
  football-data.co.uk 系只有统计没有赔率）。退一步测「角球本身可不可预测」：
  相对区分力 1.8%，进球 1.9%，**基本一样**。原材料并不更多，而角球盘抽水
  7-10%、限额几百块，比 1X2 更难翻正
- 低级别联赛定价更软——27 个池全部证伪
- 概率校准层（isotonic / temperature scaling）——只改善 0.00004
- 策略层全部变量（仓位、EV 门槛、串关、联赛、赔率来源、赔率区间）——08 号已排除

**不要在没读 09 号文档的情况下继续开发「提高盈利」相关的功能。**
特别是：EV 门槛越高 ROI 越差是单调且已确认的（扣掉抽水后的净贡献
-0.35% → -1.80% → -3.73% → -4.87% → -7.99%）——EV 筛选器不是在挑价值，
是在挑模型自己的误差，加高门槛只会亏得更快。

判定标准（跟用户约定过，不要改）：模型 RPS 必须低于市场 RPS，且平注 ROI > 0，两条都过才算通过。
补充一条方法要求：任何新结论都必须给出配对标准误。08 号那轮 659 注的模拟
不足以支撑它当时下的拆解结论（方向后来被 95,318 注证明是对的，但当时的证据不够）。

## 开始工作前必读

**先读 `handoff/00_START_HERE.md`，再读 `handoff/01_CURRENT_STATE.md`。**

这个项目由一个 claude.ai 对话开发到现在，那个对话你看不到。`handoff/` 里 7 份文档是全部上下文来源。不要跳过直接看代码——项目后期经历过两次环境重置，部分「文档里提到做过的功能」的代码并不在仓库里，`01_CURRENT_STATE.md` 用 ✅/⚠️/❌ 明确标注了每一项的真实状态。

## 硬性约束（不要越过）

- **不做自动下注。** 「实盘」功能的定义是：用户自己在博彩平台下完注后，回来手动登记。不写自动登录博彩账户、自动提交投注的代码。理由见 `00_START_HERE.md`。
- **EV 公式不能改成用市场赔率反推概率。** `EV = 独立模型概率 × 市场原始赔率 - 1`。用去抽水后的市场概率去算是循环论证，项目早期犯过这个错并彻底重构掉了。抽水率可以作为展示信息，但不进公式。
- **不往「稳赚」「必胜」方向包装。** 项目基调是精算式的诚实——赌球长期负期望，系统价值在于算清楚这一注数学上是否合理。

## 工作方式要求

**写完代码必须实际运行验证，不能只做语法检查就宣布完成。**

这个项目至今抓到的 12 个真实 bug（见 `handoff/04_BUGS_AND_LESSONS.md`），**没有一个是靠代码审查发现的**——贝叶斯更新方向反了、皇马被拆成两支球队、数据库并发冲突，全是真的跑起来测试才暴露的。请继承这个习惯：起服务、发真实请求、写带明确预期结果的断言。

改动涉及以下部分时，先跑对应验证：
- `defense` 参数 → 跑「零封后防守力应上升、大量失球应下降」的方向性测试
- 队名归一化 → 跑别名映射表的回归测试
- 数据抓取 → 用真实网络请求验证，不要用 mock

## 常用命令

```bash
# 后端
cd backend && pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000

# 前端
cd frontend && npm install && npm run dev

# MLE 训练（正常不需要重跑）
cd backend/training && python3 train_mle.py
```

API 交互式文档：http://127.0.0.1:8000/docs

## 当前状态

已完成：P0.1（回测按赛事拆分）、俱乐部 MLE 参数（四大联赛+欧冠合并训练，164队）、欧冠接入、P1.1（参数表按作用域切换）、串关记账、资金曲线修复、一键启动。

最大的开放问题不再是工程，是**模型精度**——见上方"最重要的事实"。
