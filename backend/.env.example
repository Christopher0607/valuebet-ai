# Railway 上要配的三个环境变量（项目 → Variables）
# 本地开发一个都不用配，不配就是 SQLite + 无认证。

# Supabase → Project Settings → Database → Connection string (URI)
# 注意：要用 Connection Pooling 那个（端口 6543），不是直连的 5432——
# Railway 的实例会反复重启，直连会很快耗尽 Supabase 的连接数上限。
DATABASE_URL=postgresql://postgres.xxxx:密码@aws-0-区域.pooler.supabase.com:6543/postgres

# 验签方式二选一，看你的 Supabase 项目是哪一代：
#
# 【新项目，推荐】非对称签名。设置页只有 "JWT Signing Keys"、没有 "JWT Secret"
# 时就是这种。填项目 URL 即可，后端自己去取公钥，不需要任何私密值。
SUPABASE_URL=https://xxxxx.supabase.co
#
# 【旧项目】对称签名。设置页有 "JWT Settings → JWT Secret" 时用这个。
# 这是私密值，绝对不要放进前端。
# SUPABASE_JWT_SECRET=你的jwt密钥

# Vercel 给的前端域名，逗号分隔可写多个
FRONTEND_ORIGINS=https://你的项目.vercel.app
